import { SvgSheetConfig } from '@model/config';

export const defaultConfig: SvgSheetConfig = {
    fontSize: 52,
    padding: 50,
    barline: {
        strokeWidth: 0.5,
    },
    stave: {
        marginLeft: 0.5
    }
}