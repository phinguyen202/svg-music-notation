import { AccidentalKeySignature } from '@model/business.model';

export default new Map<AccidentalKeySignature, number[]>([
    ['flat', [25, 10, 30, 15, 35, 20, 40]],
    ['sharp', [5, 20, 0, 15, 30, 10, 25]],
]);